<%=content_tag :h1, "Edit user #{user}" %>

<%= simple_form_for(user, html: { multipart: true } ) do |f| %>
  <%= render partial: 'form', object: f %>
  <%= f.button :wrapped, cancel: ( user.id == current_user.id ? home_path : users_path ) %>
<% end %>

