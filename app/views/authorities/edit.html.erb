<h1>Editing authority</h1>

<% form_for(@authority) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @authority %> |
<%= link_to 'Back', authorities_path %>