<%= form.autocompleted_input :user_name, :label => "User",
:url => form.object.motion.persisted? ? allowed_motion_users_path( form.object.motion, :format => :json ) : users_path( :format => :json ) %>

