<h1>New request</h1>

<% semantic_form_for(:request, @request, :url => position_requests_path(@request.position) ) do |f| %>
  <%= render :partial => 'form', :object => f %>
<% end %>

<p>
<%= link_to 'Back', position_requests_path( @request.position ) %>
</p>

