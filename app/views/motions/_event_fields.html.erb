<%= event_fields.input :event_date, as: :date_picker,
  label: "#{action_name.titleize} date",
  hint: 'What is the effective date for this action?' %>
<%= event_fields.input :event_description, as: :string,
  hint: 'Any additional information about this action for the log.' %>

