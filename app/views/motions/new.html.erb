<%=content_tag :h1, "New motion for #{motion.committee}" %>

<%= simple_form_for( motion, url: committee_motions_path(motion.committee) ) do |f| %>
  <%= render partial: 'form', object: f %>
<% end %>

