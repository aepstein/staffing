Dear <%= vicechairs.map(&:first_name).listify %>,

<% if motion.sponsored? %><%= motion.users.map(&:name).listify %> proposed <%= motion.to_s(:numbered) %><% else %><%= motion.to_s(:numbered) %> was proposed<% end %> on <%= event.occurrence.to_s :long_ordinal %> for consideration by <%= motion.committee %>.

You may <%= motion.status_paths.events.map(&:to_s).listify(joiner: 'or') %> the motion here:

<%= motion_url(motion) %>

<%= motion.effective_contact_name %>
<%= motion.effective_contact_email %>

